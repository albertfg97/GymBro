# Agent Rules

## Commit discipline
- All commit messages must begin with 'AI - '.
- Commits can be made freely, but pushing requires the user's command.

## Push discipline
- NEVER push unless the user explicitly says "push".
- If asked to do something that would normally end with a push, commit first and wait for the user to say "push".
